<%= form_with(model: tipo_perfil) do |form| %>
  <% if tipo_perfil.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_perfil.errors.count, "error") %> prohibited this tipo_perfil from being saved:</h2>

      <ul>
        <% tipo_perfil.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tipo, style: "display: block" %>
    <%= form.text_field :tipo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
