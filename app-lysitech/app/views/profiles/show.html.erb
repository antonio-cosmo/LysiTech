<section>
  <p style="color: green"><%= notice %></p>

  <%= render @profile %>

  <div>
    <%= link_to "Editar usuario", edit_profile_path(@profile) %> |
    <%= link_to "Listar usuario", profiles_path %>

    <%= button_to "Excluir usuario", @profile, method: :delete %>
  </div>
</section>
